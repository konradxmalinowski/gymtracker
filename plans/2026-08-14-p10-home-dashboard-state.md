# P10 state

Last updated: 2026-08-18 (resumed after session interruption; Step 4.1 confirmed complete on disk, Step 4.2 dispatched to finish the remainder)

## Current step

Step 10 done. Ready for Step 11 (commit) - awaiting explicit user go-ahead before staging/committing anything (standing git-safety rule: never commit without being asked). Step 12 (push/PR) needs its own separate explicit approval afterward, per the plan's own note.

## Agent dispatch status

| Step | Status | Summary |
|---|---|---|
| Step 0 - clarifying questions | done | User decided: lightweight home-owned queries (not a pulled-forward StatisticsRepository); simple-rotation next-suggested-day; Quick Start stays empty-only; dev-client build deferred again. |
| Step 1/1b - sync + branch | done | `main` fast-forwarded 5e004cf -> 5e43904 (P9 PR #13 + nanoid-fix PR #14 both merged remotely, local was behind). Branch `feat/p10-home-dashboard` created off updated `main`. |
| Step 2/2a/2b/2c - context | done | CLAUDE.md/ROADMAP.md/ARCHITECTURE.md read. No environment/toolchain mismatch found (dev-client gap is pre-existing and deferred by user choice, not a new mismatch). architecture-snapshot.md's `snapshot_commit` header is stale (`TBD-at-commit-time` placeholder left from P9) - flagged for the Step 10 docs pass to fix, not fixed here. Repo description/topics already set on GitHub - no-op. |
| Step 3/3b - plan + state | done | `plans/2026-08-14-p10-home-dashboard.md` and this file. |
| Step 4.1 - backend-agent-sonnet | done (confirmed on resume, was mid-session when session was interrupted; state file had stale "queued" status) | Domain (`StreakCalculator`, `nextSuggestedPlanDay`, both with tests), `HomeDashboardRepository`/`SqliteHomeDashboardRepository` (with `NodeSqlExecutor` integration tests), container wiring, `eslint.config.js` FEATURES list, `__tests__/database/helpers/fixtures.ts` extended for new session fields. All present on disk and verified by reading. |
| Step 4.2 - frontend-agent | done | Added missing `home.*` i18n keys (fixed a typecheck-blocking gap left from before the interruption); built `LatestPRCard`, `WeeklySummaryCard`, `HomeScreen.tsx`; reduced `app/(tabs)/index.tsx` to a thin wrapper; built `app/(modals)/plan-day-picker.tsx` (self-sufficient - owns its own `startFromPlanDay` call, pops itself off the stack once the push succeeds, own blocked-session dialog); extended `features/home/index.ts` barrel with hook/component exports; fixed a pre-existing `Screen.tsx` type bug (`refreshControl` typed as bare `ReactElement`, didn't satisfy `ScrollView`'s prop type - first screen to actually use it). `npx tsc --noEmit`/`eslint`/`prettier --check`/`expo export --platform ios` all verified clean (re-confirmed by orchestrator on resume, not just agent's own claim). |
| Step 4.3 - test-agent | done | 8 new test files (5 cards, hook, screen, plan-day-picker modal), 38 new tests. Regression-test discipline applied to the modal's self-pop-after-start behavior (neutered the fix, confirmed the test failed, restored it, confirmed green - same discipline P7/P8/P9 used). No real production bugs found. `app/(tabs)/index.tsx` left untested by design (codebase precedent: thin tab wrappers are never tested directly). Full suite independently re-run by orchestrator: 123 suites, 1130 passed, 1 pre-existing skip - matches agent's own report. |
| Step 6 - code review | done | `/code-review high` against `main` found 5 findings, all fixed by the orchestrator directly (not re-delegated - well-scoped, root-caused with certainty via source inspection). (1) **Real navigation bug, worse than the reviewer's own framing**: `plan-day-picker.tsx`'s original "push workout/active, then self-pop via `router.back()`" design was broken on the resume-from-blocked path (the guard-timing gap the reviewer found) AND, on inspection of `node_modules/expo-router`'s actual `goBack` implementation, broken in principle on the direct-start "happy path" too - `goBack`'s bubbling semantics target whichever screen is *currently focused* (the just-pushed `workout/active`), not the picker underneath it, so `router.back()` would have dismissed the workout screen, not the picker. Confirmed by cross-checking `node_modules/expo-router/build/global-state/router.js` and this codebase's own already-proven `router.replace` cross-group precedent (`useFinishDiscardWorkout`'s `finish()`/`discard()`, ADR-0007 rule 3). Fixed by adding a `{ navigation: 'push' \| 'replace' }` option to `useStartWorkout`'s `startFromPlanDay`/`startEmpty`/`resumeBlocked` (default `'push'`, unchanged for every other caller) and having the picker use `'replace'` for both the direct-start and resume-blocked paths - one atomic navigation action, no push-then-pop ordering to get right, no window where both screens are on the stack. (2) **`app/(modals)/plan-day-picker.tsx` violated CLAUDE.md's "app/ never contains screen bodies" rule** - extracted the real logic into `features/home/screens/PlanDayPickerScreen.tsx`, reduced the app/ file to a thin wrapper matching `exercise-picker.tsx`/`rest-timer-settings.tsx`'s precedent. (3) `useHomeDashboard.ts`'s two plan-dependent reads (`getPlan`, `getMostRecentCompletedPlanDayId`) ran sequentially despite neither depending on the other - now run via `Promise.all`. (4) `useHomeDashboard.ts` duplicated `StreakCalculator.ts`'s calendar-math primitives verbatim - `StreakCalculator.ts` now exports `ONE_DAY_MS`/`parseLocalDate`/`formatLocalDate`, `useHomeDashboard.ts` imports them (same file's `calculateStreak` behavior/tests unchanged). (5) `formatHomeDuration.ts` duplicated `workout-logging`'s `formatSessionDurationSeconds` byte-for-byte - added that export to the `workout-logging` barrel, `formatHomeDurationSeconds` now delegates to it. |
| Step 6a - edge cases | done | Re-verified against the plan's own edge-case list during the fix pass - no active plan (unchanged), single-day plan hides "change day" (unchanged, `ActivePlanCard`'s existing guard), soft-deleted plan-day fallback (unchanged, `nextSuggestedPlanDay`'s own existing test), workout-finished-before-dashboard-loads invalidation (unchanged, uses `homeDashboardKeys.dashboard` correctly). The blocked-session double-navigation case (new) is now real regression-tested. |
| Step 7 - run/verify | done | `npx tsc --noEmit` clean, `npx eslint .` clean (0 errors, pre-existing require-import warnings only), `npx expo export --platform ios` bundled successfully - all independently re-run by the orchestrator after the review-fix pass, not just trusted from an agent report. Dev-client build deferred again, per Step 0 decision (flagged again per standing feedback memory). |
| Step 8 - tests | done | Full suite: 123 suites, 1130 passed, 1 pre-existing skip - independently re-run twice by the orchestrator (once after Step 4.3, once after the Step 6 fix pass). New `__tests__/features/home/screens/PlanDayPickerScreen.test.tsx` (7 tests, replacing the deleted `__tests__/app/(modals)/plan-day-picker.test.tsx` - moved to test the extracted screen directly rather than the now-thin app/ wrapper, matching this codebase's "thin wrappers aren't tested directly" precedent) includes a real regression test for the router.replace fix and a new resume-path test that would have caught the original bug. |
| Step 9 - security-agent-sonnet | done | `reports/security-2026-08-18-p10.md`. 0 critical/high/medium/low, 3 informational. All new SQL fully parameterized, `deleted_at IS NULL` filtering consistent with sibling repositories, no injection surface in the new route/query-key handling, `useStartWorkout`'s navigation option confirmed additive/non-regressing for existing callers, no new dependency. Nothing fixed (nothing to fix). |
| Step 9e - accessibility-agent | done | `reports/accessibility-2026-08-18-p10.md` (general-purpose stand-in, per this project's established substitution). 0 BLOCKING, 2 non-blocking (HIGH), both fixed same-pass with real regression tests (revert-and-confirm discipline): **A11Y-P10-001** `PlanDayPickerScreen.tsx` never announced loading/empty state via `AccessibilityInfo` (unlike this same phase's `HomeScreen.tsx`) - fixed. **A11Y-P10-002** the day-row's `onPress={isStarting ? undefined : ...}` pattern took the whole row out of `ListRow`'s accessible branch while a start was in flight, silently discarding `accessibilityRole`/label/`busy` state - confirmed via RNTL prop dump, fixed by gating on `disabled` alone (matching `Button.tsx`'s own `loading` pattern) instead of nulling `onPress`. The `SwipeableRow`-collapse bug class (P7/P8's recurring finding) confirmed absent from this feature entirely (`SwipeableRow` is never imported anywhere under `features/home`). A pre-existing, lower-risk instance of the same `onPress`-nulling pattern was noted in `features/plans/screens/PlanDetailScreen.tsx:188` for awareness only - out of scope (not a P10 file), not fixed. Test count: `__tests__/features/home` now 70 tests across 11 suites (was 68). Independently re-verified by orchestrator: typecheck/lint clean, full suite 123 suites / 1132 passed / 1 pre-existing skip. |
| Step 9b/9c/9d | skipped | No web surface (9b, 9d crawler portion); no infra/CI change (9c); no public API added (9d API portion). |
| Step 10 - docs-agent | done | `CLAUDE.md`, `CHANGELOG.md`, `docs/ARCHITECTURE.md` (sections 8.3/9/9.1/10.1), `docs/architecture-snapshot.md` (P9's stale `TBD` header replaced with a P10 `TBD-at-commit-time` placeholder - real hash still needs filling in after Step 11's commit, matching the P7/P8 precedent the file's own "Note on snapshot_commit" paragraph documents), new `docs/adr/0019-home-dashboard-read-model.md` (the `StatisticsRepository` deviation, with a P11 migration note). Independently re-verified: typecheck clean, full suite still 123 suites/1132 passed/1 skip after the docs pass. Two pre-existing, P10-unrelated doc-sync gaps noted by the agent but left untouched (section 8.4's aspirational `AppContainer` sample; section 12.1 missing a P9 `invalidateAfterHistoricalEdit` row) - flagged for a future pass, not this phase's problem. |
| Step 11 - commit | blocked on explicit user approval | 3-commit split ready (domain+repo, UI+bugfix, docs - see "Commit split" below for the finalized file list, adjusted from the original plan now that the review pass touched `workout-logging` files too). Per this session's standing git-safety rule, nothing will be staged/committed until the user explicitly says so. |
| Step 12 - push/PR | not started | Requires its own separate explicit user approval, after Step 11. |

## Files changed so far

Domain/repository (Step 4.1): `features/home/domain/{StreakCalculator,nextSuggestedPlanDay}.ts` + tests, `features/home/repository/{HomeDashboardRepository,SqliteHomeDashboardRepository}.ts` + tests, `services/container.ts`, `eslint.config.js`, `__tests__/database/helpers/fixtures.ts`.

UI (Step 4.2 + Step 6 fixes): `features/home/hooks/useHomeDashboard.ts`, `features/home/components/{ActivePlanCard,LastWorkoutCard,StreakCard,LatestPRCard,WeeklySummaryCard,formatHomeDuration}.{ts,tsx}`, `features/home/screens/{HomeScreen,PlanDayPickerScreen}.tsx`, `features/home/index.ts`, `app/(tabs)/index.tsx` (thin wrapper), `app/(modals)/plan-day-picker.tsx` (thin wrapper), `i18n/catalogs/en.ts`, `components/layout/Screen.tsx` (refreshControl prop + a type-correctness fix), `navigation/routes.ts`, `features/workout-logging/hooks/invalidation.ts`, `features/workout-logging/hooks/useStartWorkout.ts` (navigation option), `features/workout-logging/index.ts` (formatSessionDurationSeconds export).

Tests (Step 4.3 + Step 6 fix pass): `__tests__/features/home/{components/*,hooks/useHomeDashboard,screens/HomeScreen,screens/PlanDayPickerScreen}.test.tsx`. `__tests__/app/(modals)/plan-day-picker.test.tsx` deleted (superseded by the screens/ test, per the app/-wrappers-untested-directly precedent).

## Commit split (finalized, awaiting user approval to execute)

Adjusted from the original 3-commit preview in the plan file: the Step 6 review fix pass touched `workout-logging` files (the navigation-option bug fix), which logically belongs with the UI/screen commit, not split out on its own.

1. **`feat: add home dashboard domain calculators and read model repository`**
   `features/home/domain/StreakCalculator.ts`, `features/home/domain/nextSuggestedPlanDay.ts`, `features/home/repository/HomeDashboardRepository.ts`, `features/home/repository/SqliteHomeDashboardRepository.ts`, `__tests__/features/home/domain/StreakCalculator.test.ts`, `__tests__/features/home/domain/nextSuggestedPlanDay.test.ts`, `__tests__/features/home/repository/SqliteHomeDashboardRepository.test.ts`, `services/container.ts`, `eslint.config.js`, `__tests__/database/helpers/fixtures.ts`.

2. **`feat: add home dashboard screen with plan suggestion, streak and weekly summary`**
   `features/home/index.ts`, `features/home/hooks/useHomeDashboard.ts`, `features/home/components/*`, `features/home/screens/HomeScreen.tsx`, `features/home/screens/PlanDayPickerScreen.tsx`, `app/(tabs)/index.tsx`, `app/(modals)/plan-day-picker.tsx`, `navigation/routes.ts`, `i18n/catalogs/en.ts`, `components/layout/Screen.tsx`, `features/workout-logging/hooks/invalidation.ts`, `features/workout-logging/hooks/useStartWorkout.ts`, `features/workout-logging/index.ts`, every `__tests__/features/home/{hooks,components,screens}/*.test.tsx` file.

3. **`docs: document home dashboard phase and read-model decision`**
   `CLAUDE.md`, `CHANGELOG.md`, `docs/ARCHITECTURE.md`, `docs/architecture-snapshot.md`, `docs/adr/0019-home-dashboard-read-model.md`, `reports/security-2026-08-18-p10.md`, `reports/accessibility-2026-08-18-p10.md`, `plans/2026-08-14-p10-home-dashboard.md`, this state file.

**Not staged in any commit**: `Cel funkcji.md` (repo root) - the user's own scratch note for an unrelated future feature (Daily Goals & Reminders, already separately documented in `docs/ROADMAP.md` P17), not part of P10, left untouched.

After Step 11's commit lands, `docs/architecture-snapshot.md`'s `snapshot_commit` placeholder needs one small follow-up edit with the real commit hash, matching the P7/P8 precedent - not done yet since the hash doesn't exist until the commit does.

## Notes

- Full plan: `plans/2026-08-14-p10-home-dashboard.md`.
- `features/home/` is a new (13th) feature directory - `docs/ARCHITECTURE.md`
  section 9.1's dependency graph already named `home` as a `workout-logging`
  dependent; `CLAUDE.md`'s current "twelve features total" count and the
  `features/` folder listing predate this phase adding it for real. Step 10 docs
  pass updates that count.
